# Raw data policy

Raw data are ignored by default to prevent accidental publication of restricted or sensitive material. For each dataset used in the project, document its source, version, license, acquisition date, permitted sharing status, and transformation path in `03_data/` or `00_project_management/`.

The current replication archive uses freshly simulated microstates and includes no private applicant records.
